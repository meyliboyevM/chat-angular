import {Environment} from '../common/interface/environment';

export const environment: Environment = {
  production: false,
  projectMode: 'CLIENT',
  apiUrl: 'https://hetk-dev-api.xdevs.uz/api',
};
